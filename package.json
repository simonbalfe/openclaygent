{
  "name": "openclaygent",
  "version": "0.0.1",
  "description": "Open-source Claygent — a per-row web research agent. Brief in (target + prompt + schema), structured JSON out.",
  "type": "module",
  "module": "src/index.ts",
  "private": true,
  "scripts": {
    "demo": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mastra/core": "^1.41.0",
    "@openrouter/ai-sdk-provider": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
